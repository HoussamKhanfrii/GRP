# Graph Recommendation Propagation Engine (GRP)

A full-stack academic project for graph-based recommendation systems. The project is split into two clear layers:

- `c-engine/`: the C implementation of the algorithmic core.
- `dashboard/`: the React/Vite interface that visualizes the generated results.

The dashboard does not calculate recommendations itself. It reads `dashboard/public/engine-output.json`, which is generated by the C engine and served by Vite at `/engine-output.json`.

---

## Academic C Engine

The project now includes `c-engine/`, a separate C implementation of the graph-based recommendation propagation core. The existing React dashboard is unchanged and remains a visualization layer. The C engine generates compatible CSV files plus `dashboard/public/engine-output.json`, which Vite serves at `/engine-output.json` and `EngineContext.tsx` already fetches.

The C engine applies Advanced Algorithms and Data Structures concepts directly:

- `struct` records for graphs, edges, datasets, recommendations, and experiment rows
- arrays for scores, visited flags, train/test splits, and benchmark tables
- linked-list adjacency lists for the bipartite graph
- a manual queue for BFS Neighborhood Expansion
- a manual stack for explanation path reconstruction
- a manual min-heap for Top-K ranking
- `malloc`/`calloc`/`free` for dynamic memory
- Big-O comments and empirical runtime measurements
- CSV and JSON file writing from C

Compile and run the C engine:

```bash
cd c-engine
make
make run
```

Windows MinGW/MSYS2 fallback when `make` is not installed:

```bash
cd c-engine
gcc -Iinclude src/*.c -o graph_engine.exe
graph_engine.exe
```

Clean:

```bash
make clean
```

The C engine writes CSV files to `c-engine/results/` and mirrors them to root `results/`. It detects the Vite public folder and writes the dashboard payload to `dashboard/public/engine-output.json`, keeping the served path `/engine-output.json`.

## 📖 Table of Contents
1. [How Everything Works](#how-everything-works)
2. [Dashboard Interface Guide](#dashboard-interface-guide)
3. [Core Capabilities](#core-capabilities)
4. [Graph Algorithms Deep Dive](#graph-algorithms-deep-dive)
5. [Experimental Framework](#experimental-framework)
6. [Project Structure](#project-structure)
7. [Tech Stack](#tech-stack)
8. [Quickstart Guide](#quickstart-guide)
9. [Modifying the Engine](#modifying-the-engine)

---

## How Everything Works

The system follows this pipeline:

```text
C engine
  -> generates synthetic user-item interactions
  -> splits interactions into train/test sets
  -> builds a bipartite graph with adjacency lists
  -> runs graph propagation algorithms
  -> ranks candidate items with a min-heap
  -> evaluates Precision@K, Recall@K, F1@K, NDCG@K, coverage, runtime, and memory
  -> writes CSV files and dashboard/public/engine-output.json
  -> React dashboard fetches /engine-output.json and visualizes the results
```

The graph is bipartite:

```text
Users: U1, U2, U3, ...
Items: I1, I2, I3, ...
Edges: weighted interactions between users and items
```

Example:

```text
U1 -- I5
U1 -- I8
U2 -- I5
```

If the engine recommends `I12` to `U1`, an explanation path may look like:

```text
U1 -> I5 -> U8 -> I12
```

That means the recommendation was discovered through graph relationships, not from hardcoded mock data.

The important bridge file is:

```text
dashboard/public/engine-output.json
```

The dashboard loads it in:

```text
dashboard/EngineContext.tsx
```

using:

```ts
fetch("/engine-output.json")
```

## Dashboard Interface Guide

The interface is a visualization layer for the generated engine output.

### Dashboard Page

File:

```text
dashboard/pages/Dashboard.tsx
```

Shows a high-level overview:

- number of users
- number of items
- number of interactions
- graph density
- memory usage
- latest Precision@10
- runtime vs graph size chart
- method comparison table

This page mainly uses:

```text
graphStats
benchmarkResults
graphSizeExperiment
```

### Recommendations Page

File:

```text
dashboard/pages/Recommendations.tsx
```

Lets the user select:

- a target user
- a propagation method
- a Top-K value

It then displays recommended items with:

- item ID
- recommendation score
- explanation path

The method keys are:

```text
neighborhood
randomWalkRestart
spreadingActivation
weightedInfluence
```

Friendly labels are stored in:

```text
dashboard/utils/displayLabels.ts
```

### Experiments Page

File:

```text
dashboard/pages/Experiments.tsx
```

Shows experimental charts for:

- propagation depth
- graph sparsity
- graph size and memory usage

These values come from the C experiment outputs exported into `engine-output.json`.

### Graph Analysis Page

File:

```text
dashboard/pages/GraphAnalysis.tsx
```

Shows structural graph information:

- users
- items
- interactions
- density
- sparsity
- average degree
- degree distribution
- most popular items

This helps explain the shape of the graph before interpreting recommendation quality.

## 🔥 Core Capabilities

- **Synthetic Bipartite Data Generation:** A configurable C generator for users, items, weighted interactions, uniform distributions, power-law-like distributions, random seeds, and train/test splits.
- **Explainable AI (XAI) Paths:** Instead of "black-box" scores, every recommendation logs an explicit topological trace back to the user indicating exactly *why* a specific target result was generated and via which neighboring node. 
- **Runtime and Memory Reporting:** The C engine measures runtime and estimates graph memory based on allocated structures.
- **Dynamic React Dashboard:** React Context loads the generated JSON and renders metric cards, tables, charts, and recommendation paths.

---

## 🧠 Graph Algorithms Deep Dive

The core engine implements four mathematically distinct propagation approaches natively over adjacency lists:

1. **Neighborhood Expansion**
   * **Mechanism:** Breadth-first traversal evaluating multi-hop node connections explicitly outwards up to $K$ depth. 
   * **Strengths:** Excellent determinism and coverage capability across small network proximities. High processing speed for shallow topologies.

2. **Random Walk with Restart (RWR)**
   * **Mechanism:** Simulates an iterative random walker surfing network connections. The walker has a set uniform probability to teleport straight back to the original source user ($1 - \alpha$), avoiding network drifting and focusing heavily on local communities.
   * **Strengths:** Ideal for maintaining local topological relevance while escaping isolated cold-start boundaries.

3. **Spreading Activation**
   * **Mechanism:** Diffuses initial localized "activation" outward using strict decay factors ($D < 1$). Activations that drop beneath a configurable `minActivation` threshold (e.g., $1e^{-6}$) are brutally pruned from traversal memory constraints to save processing latency.
   * **Strengths:** Computationally scalable across massively connected scale-free networks.

4. **Weighted Influence Diffusion**
   * **Mechanism:** A mathematically normalized form of spreading. Adjacent routes don't just spread, they are penalized or boosted strictly relative to sum of edge degrees powered to a weight exponent factor. 
   * **Strengths:** Prevents popular hub nodes ("super-spreader items") from severely dominating inference scores (degree penalty).

---

## 🔬 Experimental Framework

We provide fully automated suites comparing methodologies natively output continuously inside `/results/*.csv` datasets:
- **Graph Size Boundaries (`experiment_graph_size`):** Scaling matrix definitions iteratively up to 10,000s of users charting the exponential breakdown of latency vs ranking ms.
- **Sparsity Constraints (`experiment_sparsity`):** Progressively removes up to 99.9% of interactions validating which algorithms survive the "Cold Start Problem."
- **Depth Constraints (`experiment_depth`):** Charts topological hop constraints (depth 1 through 5) explicitly cross-referencing latency increases versus aggregate Precision/Recall advantages.
- **Degree Imbalance:** Introduces severely weighted items determining algorithmic resilience against extreme network centralization.

`PerformanceMetrics` captures exactly standard data science evaluations inclusive of *Precision@K*, *Recall@K*, *F1*, and *NDCG@K*.

---

## 📂 Project Structure

```text
📦 GRP (Graph Recommendation Engine)
 ┣ 📂 dashboard/               # Frontend React Application
 ┃ ┣ 📂 components/            # Reusable UI widgets (Charts, Metric Cards, Tables)
 ┃ ┣ 📂 pages/                 # Full analytical views (Experiments, Graph Analysis, Recs)
 ┃ ┣ 📜 App.tsx                # Client Routing logic
 ┃ ┣ 📜 EngineContext.tsx      # Native React Context mapping the backend JSON outputs
 ┃ ┗ ...
 ┣ 📂 c-engine/                # C academic algorithmic core and CSV/JSON producer
 ┃ ┣ 📂 include/               # Header contracts for graph, heap, queue, stack, metrics
 ┃ ┣ 📂 src/                   # C implementations of propagation, ranking, export, experiments
 ┃ ┣ 📂 results/               # C-generated CSV files
 ┃ ┗ 📜 Makefile               # GCC build/run/clean targets
 ┣ 📂 src/                     # Backend Node.js / TypeScript Sandbox
 ┃ ┣ 📂 data/                  # Generates raw matrices & executes Train/Test Splitting
 ┃ ┣ 📂 evaluation/            # Profilers (Memory, precision maths)
 ┃ ┣ 📂 experiments/           # Individual scaling suites (Sparsity, Depth, Imbalance)
 ┃ ┣ 📂 graph/                 # Instantiates, formats, and manages Adjacency Node maps
 ┃ ┣ 📂 propagation/           # The algorithms (RWR, Neighborhood, Activation, etc)
 ┃ ┗ 📜 main.ts                # Main CLI Gateway triggering the core simulation routines
 ┣ 📂 results/                 # Automatically populated CSV payload directories
 ┣ 📜 tsconfig.json            # Strict TypeScript architectural settings
 ┣ 📜 vite.config.ts           # Frontend bundling limits and server configurations
 ┗ 📜 README.md
```

---

## 🛠 Tech Stack

- **Academic Algorithmic Core:** C, GCC, adjacency lists, queues, stacks, heaps, manual memory management
- **Reference/Fallback Engine:** TypeScript, Node.js, `tsx`
- **Frontend Dashboard:** React 18, Vite (Port: 5174/5173 default)
- **Visuals:** `recharts` for scalable SVG/Canvas graphic data plotting
- **File Control:** Standard IO formatting exporting to `*.csv` and dynamically parsed payloads to `engine-output.json`.

---

## 🚀 How to Run and Validate the Project

**NOTE ON MANUAL IMPLEMENTATION:** The C engine implements the graph, propagation algorithms, Top-K ranking, metrics, CSV export, and JSON export manually. It does **not** delegate recommendations to external machine learning frameworks or graph libraries.

### 1. Install Global Dependencies
```bash
npm install
```

### 2. Generate Engine Outputs With the C Core

Preferred academic C workflow:

```bash
cd c-engine
make
make run
```

On Windows without `make`, use:

```bash
cd c-engine
gcc -Iinclude src/*.c -o graph_engine.exe
graph_engine.exe
```

This writes:

```text
c-engine/results/*.csv
results/*.csv
dashboard/public/engine-output.json
```

### 3. Optional TypeScript Reference Engine

The TypeScript engine can remain as a fallback/reference:

```bash
npm run run:engine
```

### 4. Run TypeScript Experiments Exclusively
If you want to bypass the dashboard JSON generator and only compile the backend experiments and evaluation runners explicitly into CSV datasets:
```bash
npm run run:experiments
```

### 5. Launch Analytical Dashboard (Frontend)
Boot up the React local frontend environment to parse the generated payloads visually:
```bash
npm run dev
```
Navigate to your active Local port (`http://localhost:5173/` or `http://localhost:5174/`) to explore the functional UI.

### 6. Where Generated Results Are Stored
All explicit experimental outputs mapping node traversals are output strictly into the `/results` directory:
- **`benchmark_results.csv`**: A macro-aggregation displaying overall comparative metrics evaluated against all baseline topologies.
- **`experiment_depth.csv`**: Outputs the latency and ranking metrics mapped against expanding propagation hop definitions ($K=1$ through $K=5$).
- **`experiment_graph_size.csv`**: Documents structural thresholds plotting the exponent curve of algorithm survival across 100 up to 10,000 active nodes.
- **`experiment_sparsity.csv`**: Validates recommendation coverage survivability when density matrices drop iteratively toward 99.9% sparsity.
- **`experiment_degree_imbalance.csv`**: Traces specific logic penalties occurring across algorithms mathematically when testing normal datasets versus power-law (super-spreader) hubs.
- **`memory_results.csv`**: Memory estimates for graph construction and recommendation runs.
- **`quality_results.csv`**: Stores explicit mathematical `PerformanceMetrics` logic tracing *Precision*, *Recall*, *NDCG*, and *F1* scores isolated purely on topological evaluation hits.

### 7. Where `engine-output.json` Is Stored
The dynamic, pre-compiled frontend JSON object is securely structured and output strictly to: `dashboard/public/engine-output.json`

### 8. How the Dashboard Consumes `engine-output.json`
Instead of statically importing mock files, `dashboard/EngineContext.tsx` leverages a React HTTP `fetch('/engine-output.json')` asynchronously upon loading the layout. The context parses the monolithic object and distributes the `graphSizeExperiment`, `benchmarkResults`, and real-time mapping arrays downwards to the components effectively wrapping the frontend around live pipeline generations.

### 9. Verifying Generated Feedback (Absence of Mock Data)
The frontend UI displays a loading boundary until the browser retrieves `/engine-output.json`. The visible **Generated Benchmark Results** badge indicates that the dashboard is reading generated engine output rather than a hardcoded sample dataset.

---

## ⚙️ Modifying the Engine

For the C engine, start in:

```text
c-engine/src/main.c
```

The main configuration values are:

```c
const int baseUsers = 1000;
const int baseItems = 500;
const int baseInteractions = 10000;

config.maxDepth = 3;
config.decayFactor = 0.7;
config.restartProbability = 0.15;
config.maxIterations = 30;
config.minActivation = 1e-6;
config.weightExponent = 1.5;
```

After editing, rerun:

```bash
cd c-engine
gcc -Iinclude src/*.c -o graph_engine.exe
graph_engine.exe
```

The TypeScript reference engine can be adjusted in `src/main.ts`:

```ts
const generatorConfig = {
  users: 1000, 
  items: 500,
  interactions: 10000,
  degreeDistribution: "power-law", // Change to 'uniform'
  weightRange: [0.3, 1.2],
  seed: 42
};

const propagationConfig = {
  depth: 3,                  // Expand graph evaluation horizon
  decay: 0.7,
  restartProbability: 0.15,  // Directly influences RWR probabilities
  minActivation: 1e-6,       // Controls spread limits
};
```
Change these items and trigger `npm run run:engine` again.
